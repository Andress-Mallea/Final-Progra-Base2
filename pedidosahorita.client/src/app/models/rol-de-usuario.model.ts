export interface RolDeUsuario {
  RolID: number;         // Clave Primaria (PK) del rol
  NombreRol: string;     // Nombre único del rol (ej. "Administrador", "Cliente", "Vendedor", "Repartidor")
}